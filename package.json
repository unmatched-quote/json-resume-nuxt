{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "ghpages": "rm .output/public/ -rf && rm docs/ -rf && npm run generate && mkdir docs/ && cp -r .output/public/* docs/"
  },
  "devDependencies": {
    "@invictus.codes/nuxt-vuetify": "^0.2.20",
    "@nuxt/devtools": "latest",
    "@nuxtjs/tailwindcss": "^6.8.0",
    "@types/node": "^18",
    "gh-pages": "^5.0.0",
    "nuxt": "^3.6.1",
    "vite-plugin-singlefile": "^0.13.5"
  },
  "dependencies": {
    "@kurone-kito/jsonresume-types": "^0.3.7",
    "date-fns": "^2.30.0",
    "date-fns-tz": "^2.0.0"
  }
}
